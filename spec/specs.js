describe('factorial', function() {
  it ("returns 1 if 0 is input", function() {
    expect(factorial(9)).to.equal(362880);
  });

  // it ("", function() {
  //   expect( ).to.equal( );
  // });
  //
  // it ("", function() {
  //   expect( ])).to.equal( );
  // });
  //
  // it ("", function() {
  //   expect( )).to.equal( );
  // });

});
